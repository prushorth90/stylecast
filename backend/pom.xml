<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.stylecast</groupId>
    <artifactId>stylecast-backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>stylecast-backend</name>
    <description>StyleCast backend (Spring Boot)</description>

    <properties>
        <java.version>21</java.version>
        <springdoc.version>3.0.3</springdoc.version>
        <testcontainers.version>1.21.4</testcontainers.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <!-- Provides the reactive WebClient used by RetailProductSearchProvider
                 implementations to call external HTTP APIs. The app still runs as a
                 plain Servlet application (spring-boot-starter-web wins auto-detection
                 of spring.main.web-application-type when both are on the classpath);
                 this dependency is only used for its blocking-call-capable WebClient. -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>
        <dependency>
            <!-- Spring Boot 4.1 moved WebClientAutoConfiguration (the bean that
                 provides an injectable WebClient.Builder) out of spring-boot-webflux
                 into this separate module - spring-boot-starter-webflux alone does
                 NOT register a WebClient.Builder bean. Without this, injecting
                 WebClient.Builder fails with UnsatisfiedDependencyException /
                 "No qualifying bean of type ... WebClient$Builder". -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webclient</artifactId>
        </dependency>
        <dependency>
            <!-- Used ONLY by OpenAiProductDetailEnricher's narrowly-scoped product-image
                 metadata fetch (JSON-LD/og:image/gallery <img> attribute parsing) - never
                 for bulk crawling, price/inventory extraction, or any other scraping. -->
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.18.3</version>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-resttestclient</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-restclient</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>stylecast-backend</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!--
                        Force every test JVM fork to use safe, local-only values for
                        every OpenAI/weather/geocoding base URL and API key,
                        regardless of what the launching shell/CI environment
                        happens to have exported (e.g. a real OPENAI_API_KEY sourced
                        from a local .env file). This is the single, maintainable
                        choke point that guarantees no automated test can reach a
                        real external host via environment-variable configuration -
                        see also com.stylecast.testsupport.NoExternalNetworkGuardConfig
                        (fails a Spring context immediately if this is ever bypassed)
                        and src/test/resources/application-test.yml.
                    -->
                    <environmentVariables>
                        <OPENAI_API_KEY></OPENAI_API_KEY>
                        <OPENAI_API_BASE_URL>http://localhost:1</OPENAI_API_BASE_URL>
                        <WEATHER_GEOCODING_BASE_URL>http://localhost:1</WEATHER_GEOCODING_BASE_URL>
                        <WEATHER_FORECAST_BASE_URL>http://localhost:1</WEATHER_FORECAST_BASE_URL>
                    </environmentVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
